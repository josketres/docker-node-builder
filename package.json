{
  "name": "cache-npm-install-with-docker",
  "version": "1.0.0",
  "description": "An example of caching npm install with docker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp" : "3.9.1",
    "gulp-sass": "^3.0.0"
  }
}
